# KAIB2026 대시보드 리팩토링 및 복구 작업

## [x] 1. HTML 경량화 및 구조 정리
- [x] 24MB 크기의 `index.html` 내 인라인 스크립트 및 데이터 추출
- [x] 중복되거나 불필요한 스크립트 태그 제거 및 최적화
- [x] 모든 핵심 로직을 `js/` 폴더 내 모듈별로 분리 (`common.js`, `charts.js`, `app.js` 등)

## [x] 2. 데이터 로딩 및 시각화 복구 (FIXED)
- [x] 외부 데이터 파일(`js/embedded-data.js`) 연동 및 `DATA` 변수 바인딩 확인
- [x] `app.js`의 `initDashboard` 로직 복구 (드롭다운, 이벤트 리스너)
- [x] `charts.js` 모듈 내 시각화 엔진과 `app.js` 호출 체인 통합
- [x] `node -c`를 통한 자바스크립트 전 파일 구문 무결성 검증

## [x] 3. 데이터 외부화
- [x] 거대 JSON 데이터 블록들을 `js/embedded-*.js` 파일로 추출
- [x] `index.html`에서 해당 데이터 파일들을 순차적으로 로드하도록 구성

## [x] 4. 개발 환경 구축
- [x] `package.json` 생성을 통한 `npm run dev` (Vite) 환경 설정
- [x] CORS 이슈 해결을 위한 로컬 웹 서버 실행 가이드 제공

## [x] 5. 최종 환경 정리
- [x] 리팩토링 중 생성된 임시 파일 및 백업 파일 제거
- [x] `docs/20260322/` 폴더 내 최종 작업 리포트 저장
