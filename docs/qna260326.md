
1. 첨부한 KAIB_claude 폴더를 분석해줘. 
 - 첨부한 claude 폴더의 budget_db.json 과 기존에 작업한 web폴더 밑의 budget_db.json 폴더를 비교해서 구조가 어떵게 다른지 검토
 - 특히, 해당 폴더의 스크립트는 config 폴더의 config 파일을 통해 기준연도를 설정하고 있음. 해당 내용을 반영해야 년도가 바뀐 데이터도 수용이 가능함
   - 이에 따른 web폴더의 ts파일 수정 필요성 검토가 있어야함.
 - 또 해당 프로젝트에는 json - xlsx(총괄) - xlsx(A4)가 상호 연동되도록 되어 있음 (니가 만든 csv 버전의 업데이트 버전
   - convert, export로 상호 변환이 가능하게 되어 있는 부분을 반영해야함(지금은 xlsx, xslx_a4가 분리되어 있지만 통합해도 될지 검토
   - 안전을 위해 바로 buget_db를 수정하지 않고 merge.json을 만들어 확정되면 복사해서 쓰도록 구성
   - xlsx, xslx_a4는 'project' 부분을 업데이트함. 해당 부분이 업데이트되면(merge.json), project내용을 기반으로 metadata, analysis도 다시 계산하도록 함
 - 기존 budget_db.json을 보면 metadata, project, analysis로 구분됨. 현재 업데이트 되는 부분은 'project' 부분임
   - budget_db.json에서 project

2. 프로젝트 루트폴더에 input, output 폴더를 생성했다. 
 - 위에서 말한 내부작업(최종 수정된 xlsx, xslx_a4 생성)후 input폴더에 넣거나 파일지정을 해서 output폴더에 merge_날짜.json을 생성
 - 해당 merge_날짜.json을 기반으로 metadata, analysis 계산. merge_날짜_통합.json파일 생성
 - merge_날짜_통합을 기반으로 분석파일들 생성(다른 json파일들) 
 - 해당 내용이 완료되면 web/data 폴더 내용을 교체하는 로직 구성 

3. 1번에 결과에 따라 수정이 필요한 ts 파일(년도 변화)이 제대로 수정이 되는지 확인요망
 - 현재 index.html를 실행하면 나오는 각 탭들이 어떤 db나 ts 파일고 연관되어있는지 분석하고 ./data/budget_db만 사용하는 탭메뉴는 진한 파란색으로 변경해줘
		개요
		부처별 분석
		분야별 분석
		유사성 분석
		사업 목록
		정책 클러스터
		비교 분석
		미래 예산 시뮬레이터
		예산 인사이트
		AI 기술 분석
 - index파일의 상단부 "2026 AI 재정사업 분석 플랫폼 , AI-Native Budget Analytics" 이부분은 어디서 변경하는지도. 상단부 "문서"는 작동을 안하는데 어떤게 있어야 하는지도 검토

4. 해당 내용을 반영하여 폴더구조 정비. 지금은 scripts폴더에 파일들이 모아져 있는데 이제 기능에 따라 분리할 필요는 없는지.
 - 또 향후 확장성을 위해 백앤드 구축 필요성도 같이 검토(백앤드 구축을 염두에 두고 폴더구조 생성)
 
5. 완료되면 최종 구조에 맞는 가이드문서 생성(readme, guide, 업데이트된 구현계획서, 최종 프로덕트문서) 
 - 각 파일들(srripts, web/ts 폴더내)의 연관관계를 표시하고 각각파일의 역할과 db의존성(어떤 db가 있어야 작동하는지) 문서 생성
 - 유지보수 가이드라인아리고 해도 좋다. 트러블슈팅 내용까지 포함해서 완료하라. 
 - 최종 파일이 완료되면 기존 파일들(지금은 사용하지 않고 향후 참고만할)은 docs의 backup폴더로 이동하라. 
 
 
 
 
#overview, #department, #field, #duplicate, #projects, #policy, #cross-compare, #future, #insight, #ai-tech